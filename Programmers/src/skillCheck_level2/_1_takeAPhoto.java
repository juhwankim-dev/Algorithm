package skillCheck_level2;

public class _1_takeAPhoto {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

	/*
	 * public int solution(int n, String[] data) { int answer = 0;
	 * 
	 * for(int i=0; i<8; i++) { int[] seats = new int[8]; int distance =
	 * data[0].charAt(4)+1; if(i+distance < 8) { seats[0] = data[0].charAt(0); // 첫
	 * 번째 조건의 첫 번째 사람을 일단 앉혀. seats[distance] = data[0].charAt(2); // 첫 번째 조건의 두 번째
	 * 사람을 조건에 맞게 앉혀 }
	 * 
	 * int num = freeSeats(seats, n, data); // 나머지 조건에 부합했을 때 int sum = 1;
	 * while(num-- > 0) { sum *= num; } answer += sum; }
	 * 
	 * return answer; }
	 * 
	 * public static int freeSeats(int[] seats, int n, String[] data, int criteria,
	 * int pos) { if(이미 누가 앉아있으면) { return; } for(int i=1; i<n; i++) {
	 * 
	 * }
	 * 
	 * int distance = data[criteria].charAt(4)+1; seats[pos] =
	 * data[criteria].charAt(0); // 첫 번째 사람 앉히고 seats[pos2] = man2; freeSeats(seats,
	 * n, data, criteria, man1, man2, ); }
	 */
}
